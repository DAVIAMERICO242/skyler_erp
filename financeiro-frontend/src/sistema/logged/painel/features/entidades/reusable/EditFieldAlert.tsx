import styled from "styled-components"

const StyleEditFieldAlert = styled.div`
    color:gray;
`

export const EditFieldAlert = ()=>{
    return(
        <StyleEditFieldAlert>
            {"[a editar]"}
        </StyleEditFieldAlert>
    )
}